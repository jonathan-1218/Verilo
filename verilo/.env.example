SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
# Optional: enables the opt-in cloud transcription fallback (Deepgram Nova-3).
# Leave empty to keep the feature off.
DEEPGRAM_API_KEY=
# Optional: crash/error reporting (sentry.io project DSN). Leave empty to disable.
SENTRY_DSN=
